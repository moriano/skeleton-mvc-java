machine:
  java:
    version: openjdk7
checkout:
  post:
    - git submodule sync
    - git submodule update --init

general:
  artifacts:
    - "target/skeleton.war" # War!

deployment:
  master:
    branch: master
    owner: moriano
    commands:
      - curl -X POST https://manageacloud.com/trigger/2s1aegf4kedc7n6ihnnblcpo23
