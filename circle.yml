machine:
  java:
    version: openjdk7
checkout:
  post:
    - git submodule sync
    - git submodule update --init

general:
  artifacts:
    - "skeleton.war" # The war file!
    - "/home/ubuntu/skeleton-mvc-java/target/skeleton.war" # War!
    - "target/skeleton.war" # War!

notify:
  webhooks:
    # A list of hook hashes, containing the url field
    - url: https://manageacloud.com/trigger/2s1aegf4kedc7n6ihnnblcpo23
